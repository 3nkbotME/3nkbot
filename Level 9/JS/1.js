(b)
function element2(arr) {
    return arr.filter(function(element) {
      return element % 2 === 0;
    });
  }
  
  const originalArray = [5, 12, 8, 15, 3, 20];
  const resultArray = element2(originalArray);
  console.log(resultArray);
(A)
  function element(arr) {
    return arr.filter(function(element) {
      return element > 10;
    });
  }
  
  
  const originalA = [5, 12, 8, 15, 3, 20];
  const resultA = element(originalArray);
  console.log(resultArray); 











  (D)

